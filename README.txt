Andrew Massey (811238661)
Jospeh Clements(811830670)

1730 Editor:

To compile: type "make" into the command line

To run: type "./cs1730" along with two options into the command line:
    1) a file name, which allows you to edit the file desired
    2) nothing, which allows you to create a new file

 
To clean: type "make clean" into the command line
